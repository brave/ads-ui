fragment Geocode on Geocode {
  code
  name
}

fragment Segment on Segment {
  code
  name
}

query ActiveGeocodes {
  geocodes {
    ...Geocode
  }
}

query Segments {
  segments {
    data {
      code
      name
    }
  }
}
